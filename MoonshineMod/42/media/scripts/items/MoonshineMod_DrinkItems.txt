

module Moonshine
{



imports
  {
    Base
  }
 
 	








    /* =====================================================================
       FILLED-VESSEL WEIGHTS -- container + contents   (BACKLOG 3.19 sweep,
       2026-09-06).  Companion to the bucket-family pass in 8ad8f08; this is
       the "once and for all" mod-wide alignment to vanilla.

       THE ENGINE RULE (CFR 42.20 + measured live on mvfast, not estimated):
         InventoryItem.getUnequippedWeight() = getActualWeight()
                                             + getContentsWeight()
         and getContentsWeight() for a component FluidContainer returns
         fluidContainer.getAmount() VERBATIM -- the engine's liquid density is
         exactly 1.0 kg per litre, for water, spirit, bleach and petrol alike
         (Base.PetrolCan: 1.6 + 10 L = 11.6, so vanilla itself uses 1.0 for
         petrol).

       Every item in this file is a LEGACY base:food / base:drainable with NO
       FluidContainer component, so the engine adds nothing for contents and
       the script Weight must ITSELF be the full container+contents figure.
       It then tapers for free as the vessel empties:
         Food.getActualWeight()            -> (full-empty)*usedDelta + empty
         DrainableComboItem.updateWeight() -> (full-empty)*uses      + empty
       with `empty` read from ReplaceOnUse / ReplaceOnDeplete.  One authored
       number sets both ends.

       EVERY target below is the vanilla vessel's own filled reading, MEASURED
       by filling it to its declared FluidContainer capacity on the live
       server -- see tools/pzrepl/results/vesselweight-anchors-2026-09-06.txt:

         Base.Whiskey      0.7 + 1.0 L = 1.70     Base.EmptyJar  0.2 + 1.0 = 1.20
         Base.BeerEmpty    0.1 + 0.3 L = 0.40     Base.Bleach    0.2 + 1.0 = 1.20
         Base.WineOpen     0.3 + 1.0 L = 1.30     Base.Kettle    1.0 + 1.5 = 2.50
         Base.PopBottle    0.1 + 2.0 L = 2.10     Base.Saucepan  0.7 + 0.6 = 1.30
         Base.WaterBottle  0.1 + 1.0 L = 1.10     Base.Mugl      0.2 + 0.2 = 0.40
         Base.Sportsbottle 0.1 + 1.0 L = 1.10     Base.BucketEmpty 1.0+10  = 11.0
                                                  Base.WateredCan  2.0+ 8  = 10.0

       NOT everything went up: a beer bottle is 330 ml, so every *Beer* vessel
       DROPS 0.7 -> 0.4, and SaucepanWaterRefill drops 2.0 -> 1.3 (the pan only
       holds 600 ml).  The 2-litre pop bottle is the biggest single rise,
       0.2 -> 2.1.

       A cross-check that deliberately did NOT win: vanilla's own sealed
       Base.CannedCarrots ("Jar of Carrots") is 0.8, not 1.2.  That is a
       solid-pack jar and vanilla's food weights are loose; the FluidContainer
       route is the one the player actually sees, because a vanilla jar filled
       with water at a sink reads exactly 1.20.  Jars of liquid follow the jar
       of liquid.

       Guard: tools/pzrepl/tests/vesselweight.lua asserts every number here.
       ===================================================================== */

/* refills */
	
	
		item WhiskeyRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,
		
		/* container+contents (BACKLOG 3.19): was 0.7 -> 1.7.
		   Base.Whiskey 0.7 + 1.0 L; a filled vanilla whiskey bottle reads 1.70 */
		Weight	=	1.7,
		
		ItemType	= base:food,
		FoodType = Liquor,
		
		UnhappyChange	=	-50,
		ThirstChange	=	-10,
		HungerChange	=	-10,
		
		DisplayName	= Whiskey Bottle filled with Spirit,
		ReplaceOnUse	= Whiskey,
		/*ReplaceOnDeplete = */
		Alcoholic	=	TRUE,
		AlcoholPower = 2,
		Icon	=	Whiskey,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,
		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 1200,
		Packaged = TRUE,
        CantBeFrozen = TRUE,
        StaticModel = WhiskeyBottle,
        WorldStaticModel = WhiskeyBottle,
		UseDelta = 0.25,
		Tags = Liquor,
	}
	
	
	

	item BeerRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,
		HungerChange	=	-9,
		/* container+contents (BACKLOG 3.19): was 0.7 -> 0.4.
		   Base.BeerEmpty 0.1 + 0.3 L; a filled vanilla beer bottle reads 0.40 -- a beer bottle is 330 ml, so this DROPS */
		Weight	=	0.4,
		
		ItemType	= base:food,
		FoodType = Liquor,
		
		UnhappyChange	=	-30,
		ThirstChange	=	-13,
		DisplayName	=	Beer Bottle filled with Spirit,
		
		ReplaceOnUse = BeerEmpty,
		Alcoholic	=	TRUE,
		AlcoholPower = 2,
		Icon	=	BeerBottle,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,
        
		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 1200,
		
	
        Packaged = TRUE,
        CantBeFrozen = TRUE,
        StaticModel = BeerBottle,
        WorldStaticModel = BeerBottle,
		UseDelta = 0.25,
		Tags = Liquor,


	}

	item WineRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,
		HungerChange	=	-9,
		/* container+contents (BACKLOG 3.19): was 0.7 -> 1.3.
		   Base.WineOpen 0.3 + 1.0 L; a filled vanilla wine bottle reads 1.30 */
		Weight	=	1.3,

		ItemType	= base:food,
		FoodType = Liquor,

		UnhappyChange	=	-30,
		ThirstChange	=	-13,
		DisplayName	=	Wine Bottle filled with Spirit,

		ReplaceOnUse = WineOpen,
		Alcoholic	=	TRUE,
		AlcoholPower = 2,
		Icon	=	Wine,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,

		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 1200,


        Packaged = TRUE,
        CantBeFrozen = TRUE,
        StaticModel = WhiteWineBottle,
        WorldStaticModel = WineWhiteGround,
		UseDelta = 0.25,
		Tags = Liquor,


	}


	item WhiteLightning
    {
	StressChange	=	-99,
	HungerChange	=	-6,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L; a vanilla jar filled at a sink reads 1.20 */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Appalachian White Lightning,
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.1,
	UnhappyChange	=	-99,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,


    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 686,
	
	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	/* ------------------------------------------------------------------
	   Flavored White Lightning (v1: WhiteLightning jar only).
	   Same booze (AlcoholPower/ThirstChange held at the WhiteLightning
	   baseline), softened StressChange/UnhappyChange -99 -> -85: that IS
	   the flavor mechanic.  Deliberately NON-perishable like the parent
	   (Packaged = TRUE, no DaysFresh/DaysTotallyRotten) even though the
	   fruit input is perishable.

	   SPOILAGE INHERITANCE -- verified headlessly 2026-09-05
	   (tools/pzrepl/tests/flavor.lua): a craft output does NOT inherit its
	   inputs' age or rot.  Both behaviours are opt-in per-input flags that
	   these recipes deliberately do not set:
	     flags[InheritFoodAge]   -- would copy the fruit's age onto the jar
	     flags[AllowRottenItem]  -- would let a rotten fruit be used at all
	   Measured: fresh apple and a 7.5-day apple both yield age=0,
	   OffAge/OffAgeMax = 1000000000 (the "never spoils" sentinel, straight
	   off this script, identical to the parent WhiteLightning), and
	   setAge(100000) on the result still reads isRotten() == false.
	   A fully rotten fruit simply makes the recipe UNAVAILABLE.
	   ------------------------------------------------------------------ */

	item WhiteLightningApple
    {
	StressChange	=	-85,
	HungerChange	=	-7,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Appalachian White Lightning (Apple),
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.1,
	UnhappyChange	=	-85,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 740,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	item WhiteLightningCherry
    {
	StressChange	=	-85,
	HungerChange	=	-7,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Appalachian White Lightning (Cherry),
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.1,
	UnhappyChange	=	-85,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 700,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	item WhiteLightningPeach
    {
	StressChange	=	-85,
	HungerChange	=	-7,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Appalachian White Lightning (Peach),
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.1,
	UnhappyChange	=	-85,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 730,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	item WhiteLightningLemon
    {
	StressChange	=	-85,
	HungerChange	=	-7,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Appalachian White Lightning (Lemon),
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.1,
	UnhappyChange	=	-85,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 700,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	item WhiteLightningMint
    {
	StressChange	=	-85,
	HungerChange	=	-6,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Appalachian White Lightning (Mint),
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.1,
	UnhappyChange	=	-85,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 690,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	/* ------------------------------------------------------------------
	   BACKLOG 1.4 -- WHISKEY BY AGING, a 3-STAGE LADDER.

	     craftRecipe CharAndAgeWhiteLightning
	       Moonshine.WhiteLightning + 2 charcoal
	         -> WhiteLightningCharred  "Charred Oak Shine (1/3)"
	         -> (30 days, unattended)  WhiskeyGreen "Green Whiskey (2/3)"
	         -> (90 days total)        WhiskeyScarletOak "Scarlet Oak Whiskey"

	   Only the FIRST step is a craft.  The other two are `ReplaceOnRotten`
	   swaps driven by the item's own age -- quirks rule 69, which proves the
	   engine is completely indifferent to the DIRECTION of the swap (vanilla's
	   own SugarBeetSyrupPot -> SugarBeetSugarPot is the same positive shape).
	   `Moonshine.WhiteLightning` itself is deliberately UNCHANGED and stays
	   non-perishable; nothing here alters the flagship jar.

	   THREE HARD CONSTRAINTS, all measured live 2026-09-06
	   (tools/pzrepl/probes/probe_aging_ladder.lua, 63 assertions, 0 failures):

	   (a) EVERY `ReplaceOnRotten` TARGET MUST BE `ItemType = base:food`.
	       Anything else makes InventoryItemFactory.CreateItem() return null and
	       the SOURCE IS DESTROYED WITH NOTHING CREATED -- a silent vanish with
	       only a server-log WARN.  Verified here as a deliberate control.  All
	       three items below are base:food; verify any future link the same way.

	   (b) THE TARGET INHERITS THE SOURCE'S `age`, so each stage needs a
	       strictly LARGER `DaysTotallyRotten` than the age it inherits, or the
	       whole ladder would cascade in a single tick.  30 -> 90 -> none.
	       Measured: stage 2 inherits age 30.5 against its own 90, not rotten.

	   (c) THE TARGET'S NUTRITION IS OVERWRITTEN BY THE SOURCE'S:
	       HungerChange, UnhappyChange, BoredomChange, Calories, Carbohydrates,
	       Lipids, Proteins all arrive from the previous stage.  So all three
	       declare IDENTICAL nutrition (the WhiteLightning baseline) and the
	       ladder is differentiated ONLY through StressChange, AlcoholPower and
	       DisplayName -- fields the target demonstrably KEEPS.  Measured: a
	       source with Calories forced to 9999 produced a stage 2 reading 9999
	       calories but stage 2's own StressChange -0.75 and AlcoholPower 1.25.
	       NEVER try to make an aged stage "better" via Calories/UnhappyChange.

	   NO `DaysFresh` ON ANY STAGE.  That keeps offAge at the 1e9 sentinel,
	   which suppresses the "(Fresh)" / "(Stale)" name prefix completely (and
	   with it every stale hunger/thirst/texture penalty).  Measured: the
	   stage-1 name is identical at ages 0 / 1 / 10 / 20 / 29.99.  The price is
	   that B42 has no freshness bar at all, which is exactly why this is a
	   3-stage ladder: the DisplayName is the ONLY progress feedback a player
	   ever gets, so the stage number lives in the name.

	   TUNING.  Aging is `age += (worldAgeHours - lastAged) * FoodRotSpeed / 24`,
	   a catch-up model, so it also runs in unloaded chunks and survives
	   save/reload.  The sandbox Food Rot Speed option scales it 0.4x-1.7x, so
	   the nominal 30 / 90 days is really 18-75 / 53-225 days, and a POWERED
	   FRIDGE applies a further 0.2x.  Both are documented in the tooltips.

	   KNOWN, ACCEPTED: a jar drunk down to half and THEN aged carries its
	   half-empty HungerChange across the swap (inherited, per (c)) but its
	   ThirstChange resets to the new stage's script value (kept, per (c)).  So
	   part-drinking an aging jar hands back the thirst half of one jar after 30
	   in-game days.  That is engine behaviour common to every ReplaceOnRotten
	   chain including vanilla's, the reward is far smaller than the wait, and a
	   workaround would cost more than it saves.
	   ------------------------------------------------------------------ */

	item WhiteLightningCharred
    {
	StressChange	=	-50,
	HungerChange	=	-6,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Charred Oak Shine (1/3),
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.1,
	UnhappyChange	=	-99,

	DaysTotallyRotten = 30,
	ReplaceOnRotten = WhiskeyGreen,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	Tooltip = Tooltip_item_WhiteLightningCharred,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 686,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	item WhiskeyGreen
    {
	StressChange	=	-75,
	HungerChange	=	-6,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Green Whiskey (2/3),
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.25,
	UnhappyChange	=	-99,

	DaysTotallyRotten = 90,
	ReplaceOnRotten = WhiskeyScarletOak,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	Tooltip = Tooltip_item_WhiskeyGreen,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 686,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }

	item WhiskeyScarletOak
    {
	StressChange	=	-99,
	HungerChange	=	-6,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight	=	1.2,
	ItemType	= base:food,
	FoodType = Liquor,
	 DisplayCategory = Food,
	DisplayName	=	Scarlet Oak Whiskey,
	ThirstChange	=	-6,

	Alcoholic	=	TRUE,
	AlcoholPower = 1.5,
	UnhappyChange	=	-99,

	ReplaceOnUse = 	Base.EmptyJar,
	Icon	=	JarEmpty2,
	Tooltip = Tooltip_item_WhiskeyScarletOak,
	CustomContextMenu = Drink,
	CustomEatSound = DrinkingFromBottle,

    Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 686,

	Packaged = TRUE,
	CantBeFrozen = TRUE,
	StaticModel = JarGlass_Ground,
	WorldStaticModel = JarGlass_Ground,
	EatType = Bourbon,
	Tags = Liquor,
	UseDelta = 0.25,
    }


	/* LEGACY / MIGRATION ONLY -- do NOT use Moonshine.EmptyJar in any new recipe.
	   It shadowed vanilla Base.EmptyJar (no FluidContainer, no base:jar/base:glass
	   tags -> could not be canned, boxed, or water-filled).  As of 2026-09-06 nothing
	   PRODUCES it any more; it survives here only so existing saves still load the
	   jars they already hold, and it stays listed in the Fill-Spirit input brackets so
	   that legacy stock drains away naturally on the next distillation. */
 item EmptyJar
	{
	    DisplayCategory = Cooking,
		ItemType	= base:normal,
		DisplayName			=		Empty Jar,
		Icon				=		JarEmpty2,
		Weight				=		0.2,
		Tooltip = Tooltip_Jar,
		SurvivalGear = TRUE,
		/* StaticModel added 2026-09-06 (BACKLOG 3.9): this was the ONLY field
		   missing, so the jar had a ground model but nothing in hand.  It is
		   also what flags[Prop1] reads -- CraftRecipeData.getModel() returns
		   input.getMostRecentItem().getStaticModel(), so an item with no
		   StaticModel makes a Prop1 flag a silent no-op. */
		StaticModel = JarGlass_Ground,
		WorldStaticModel = JarGlass_Ground,
	}
	
		
	item SodaBottleRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,
		HungerChange	=	-3,
		/* container+contents (BACKLOG 3.19): was 0.2 -> 2.1.
		   Base.PopBottle 0.1 + 2.0 L; a filled vanilla pop bottle reads 2.10 -- it is a 2-LITRE bottle */
		Weight	=	2.1,

		ItemType	= base:food,
		FoodType = Liquor,

		UnhappyChange	=	-26,
		ThirstChange	=	-3,
		DisplayName	= Soda Bottle filled with Spirit,
		/* ReplaceOnUse	= PopBottleEmpty, */
		ReplaceOnUse = PopBottle,
		Alcoholic	=	TRUE,
		AlcoholPower = 0.6,
		Icon	=	PopBottle,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,

		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 343,


        Packaged = TRUE,
        CantBeFrozen = TRUE,
        StaticModel 		= PopBottleWorld,
		WorldStaticModel 	= PopBottleWorld,
		UseDelta = 0.25,
		Tags = Liquor,
	}
	
	
	item WaterBottleRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,
		HungerChange	=	-3,
		/* container+contents (BACKLOG 3.19): was 0.2 -> 1.1.
		   Base.WaterBottle 0.1 + 1.0 L; a filled vanilla water bottle reads 1.10 */
		Weight	=	1.1,

		ItemType	= base:food,
		FoodType = Liquor,
		UnhappyChange	=	-26,
		ThirstChange	=	-3,
		DisplayName	=	Water Bottle filled with Spirit,
		/*ReplaceOnUse	= WaterBottleEmpty,*/
		ReplaceOnUse = WaterBottle,
		Alcoholic	=	TRUE,
		AlcoholPower = 0.6,
		Icon	=	WaterBottle,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,

		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 343,

		Packaged = TRUE,
        CantBeFrozen = TRUE,
        StaticModel 		= WaterBottle,
		WorldStaticModel 	= WaterBottleFull,
		UseDelta = 0.25,
		 Tags = Liquor,
	}

	item SportsBottleRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,
		HungerChange	=	-3,
		/* container+contents (BACKLOG 3.19): was 0.2 -> 1.1.
		   Base.Sportsbottle 0.1 + 1.0 L; a filled vanilla sports bottle reads 1.10 */
		Weight	=	1.1,

		ItemType	= base:food,
		FoodType = Liquor,
		UnhappyChange	=	-26,
		ThirstChange	=	-3,
		DisplayName	=	Sports Bottle filled with Spirit,
		ReplaceOnUse = Sportsbottle,
		Alcoholic	=	TRUE,
		AlcoholPower = 0.6,
		Icon	=	Sportsbottle,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,

		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 343,

		Packaged = TRUE,
        CantBeFrozen = TRUE,
        StaticModel 		= Sportsbottle,
		WorldStaticModel 	= Sportsbottle,
		UseDelta = 0.25,
		 Tags = Liquor,
	}
	
	
	
	
	
	
	
	item BleachBottleRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,
		HungerChange	=	-5,
		/* container+contents (BACKLOG 3.19): was 0.7 -> 1.2.
		   Base.Bleach 0.2 + 1.0 L; a filled vanilla bleach bottle reads 1.20 */
		Weight	=	1.2,
		
		ItemType	= base:food,
		FoodType = Liquor,
		
		UnhappyChange	=	-26,
		ThirstChange	=	-13,
		DisplayName	=	Bleach Bottle filled with Spirit,
		/*ReplaceOnUse	=	BleachEmpty,*/
		ReplaceOnUse = Bleach,
		
		
		Alcoholic	=	TRUE,
		AlcoholPower = 2,
		Icon	=	Bleach,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,
        
		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 1200,
		
		Packaged = TRUE,
        CantBeFrozen = TRUE,
		
        StaticModel = BleachBottle,
		EatType = BleachBottle,
		WorldStaticModel = BleachBottle,
		
		UseDelta = 0.25,
		 Tags = Liquor,
	}
	
	
	/*---------water items----------*/
	/*--Mugl,MugRed,MugWhite,MugSpiffo,WhiskeyEmpty/BeerEmpty/WineEmpty2/WineEmpty/EmptyJar/PopBottleEmpty/WaterBottleEmpty/BleachEmpty,--*/
	
	
item MuglWaterRefill
{
    Weight = 0.4,
    CanStoreWater = TRUE,
   ItemType	= base:drainable,
   UseWhileEquipped	= FALSE,
    DisplayName = Cyan Mug filled with Water,
    ReplaceOnUseOn = WaterSource-WaterMug,
	ReplaceOnDeplete	= Mugl,
    Icon = Mugl,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
    StaticModel = Mug,
    WorldStaticModel = Mug,
    IsWaterSource = TRUE,
    FillFromDispenserSound = GetWaterFromDispenserGlass,
    FillFromTapSound = GetWaterFromTapGlass,
    UseDelta = 1,
    CustomEatSound = DrinkingFromBottleGlass,
	IsCookable		= TRUE,
	DisplayCategory 	= Water,
}

item MugRedWaterRefill
{
    IsWaterSource = TRUE,
    Weight = 0.4,
    CanStoreWater = TRUE,
    ItemType	= base:drainable,
    UseWhileEquipped = FALSE,
    UseDelta = 1,
    DisplayName = Red Mug filled with Water,
    ReplaceOnDeplete	= MugRed,
    ReplaceOnUseOn = WaterSource-WaterMugRed,
    Icon = MugRed,
    IsCookable = TRUE,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
    StaticModel = MugRed,
    EatType = Mug,
    WorldStaticModel = MugRed,
	DisplayCategory 	= Water,
}

item MugWhiteWaterRefill
{
    Weight = 0.4,
    CanStoreWater = TRUE,
    ItemType	= base:drainable,
IsCookable		= TRUE,
    DisplayName = White Mug filled with Water,
    ReplaceOnUseOn = WaterSource-WaterMugWhite,
	ReplaceOnDeplete	= MugWhite,
    Icon = MugWhite,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
    StaticModel = MugWhite,
    WorldStaticModel = MugWhite,
    IsWaterSource = TRUE,
    FillFromDispenserSound = GetWaterFromDispenserGlass,
    FillFromTapSound = GetWaterFromTapGlass,
    UseWhileEquipped = FALSE,
    UseDelta = 1,
    CustomEatSound = DrinkingFromBottleGlass,
	DisplayCategory 	= Water,
}

item MugSpiffoWaterRefill
{
    Weight = 0.4,
    CanStoreWater = TRUE,
    ItemType	= base:drainable,
    IsCookable		= TRUE,
    DisplayName = Spiffo Mug filled with Water,
    ReplaceOnUseOn = WaterSource-WaterMugSpiffo,
	ReplaceOnDeplete	= MugSpiffo,
    Icon = MugSpiffo,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
    StaticModel = MugSpiffo,
    WorldStaticModel = MugSpiffo,
    IsWaterSource = TRUE,
    FillFromDispenserSound = GetWaterFromDispenserGlass,
    FillFromTapSound = GetWaterFromTapGlass,
    UseWhileEquipped = FALSE,
    UseDelta = 1,
    CustomEatSound = DrinkingFromBottleGlass,
	DisplayCategory 	= Water,
}
	
	
	
	/*------------------------------------------------------------*/
	
	
		
	 item BeerWaterRefill
	 
    {
	
    IsCookable		= TRUE,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
	
	DisplayCategory 	= Water,
	IsWaterSource		= TRUE,
	/* container+contents (BACKLOG 3.19): was 0.7 -> 0.4.
	   Base.BeerEmpty 0.1 + 0.3 L -- DROPS, a beer bottle is 330 ml */
	Weight			= 0.4,
	CanStoreWater		= TRUE,
	FillFromDispenserSound 	= GetWaterFromDispenserGlass,
	FillFromTapSound 	= GetWaterFromTapGlass,
	ItemType	= base:drainable,
	UseWhileEquipped	= FALSE,
	UseDelta		= 0.16,
	DisplayName		= Beer Bottle filled with Water,
	ReplaceOnDeplete	= BeerEmpty,
	ReplaceOnUseOn		= WaterSource-BeerWaterFull,
	Icon			= BeerBottle,
	StaticModel 		= BeerBottle,
	WorldStaticModel 	= BeerBottle,
	CustomEatSound 		= DrinkingFromBottleGlass,
    }
	
	
	
	
	
	
	
	
	
	 item WineWaterRefill
	 
    {

    IsCookable		= TRUE,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
	
	DisplayCategory 	= Water,
	IsWaterSource		= TRUE,
	/* container+contents (BACKLOG 3.19): was 0.7 -> 1.3.
	   Base.WineOpen 0.3 + 1.0 L */
	Weight			= 1.3,
	CanStoreWater		= TRUE,
	FillFromDispenserSound 	= GetWaterFromDispenserGlass,
	FillFromTapSound 	= GetWaterFromTapGlass,
	ItemType	= base:drainable,
	UseWhileEquipped	= FALSE,
	UseDelta		= 0.16,
	DisplayName		= Wine Bottle filled with Water,
	ReplaceOnDeplete	= WineOpen,
	ReplaceOnUseOn		= WaterSource-WineWaterFull,
	Icon			= Wine,
	StaticModel = WhiteWineBottle,
	WorldStaticModel 	= WhiteWineBottle,
	CustomEatSound 		= DrinkingFromBottleGlass,
    }
	
		
		item WhiskeyWaterRefill
		
	{
	ItemType	= base:drainable,
   
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
	
		DisplayName		= Whiskey Bottle filled with Water,
		DisplayCategory		= Water,
		ItemType	= base:drainable,
		/* container+contents (BACKLOG 3.19): was 0.7 -> 1.7.
		   Base.Whiskey 0.7 + 1.0 L */
		Weight			= 1.7,
		Icon			= Whiskey,
		CanStoreWater		= TRUE,
		EatType			= Bourbon,
		FillFromDispenserSound	= GetWaterFromDispenserGlass,
		FillFromTapSound	= GetWaterFromTapGlass,
		IsCookable		= TRUE,
		IsWaterSource		= TRUE,
		ReplaceOnDeplete	= Whiskey,
		ReplaceOnUseOn		= WaterSource-WhiskeyWaterFull,
		UseDelta		= 0.08,
		UseWhileEquipped	= FALSE,
		CustomEatSound		= DrinkingFromBottleGlass,
		StaticModel		= WhiskeyBottle,
		WorldStaticModel	= WhiskeyBottleGround,
	}
	
	

	
	
	
	
	  item BottleWaterRefill
	
        {
		
    IsCookable		= TRUE,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
	
                DisplayName = Bottle filled with Water,
                DisplayCategory = Water,
                ItemType	= base:drainable,
                /* container+contents (BACKLOG 3.19): was 0.2 -> 1.1.
                   Base.WaterBottle 0.1 + 1.0 L */
                Weight = 1.1,
                Icon	=	WaterBottle,

                CanStoreWater = TRUE,
                FillFromDispenserSound = GetWaterFromDispenserPlasticMedium,
                FillFromTapSound = GetWaterFromTapPlasticMedium,
                IsWaterSource = TRUE,
                ReplaceOnDeplete = WaterBottle,
                ReplaceOnUseOn = WaterSource-WaterBottleFull,
                UseDelta = 0.1,
                UseWhileEquipped = FALSE,
                CustomEatSound = DrinkingFromBottlePlastic,
                StaticModel = WaterBottle,
                WorldStaticModel = WaterBottleFull,
                Tooltip = Tooltip_item_OnlyPurifyMicrowave,
        }


	item SportsBottleWaterRefill
        {

    IsCookable		= TRUE,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,

                DisplayName = Sports Bottle filled with Water,
                DisplayCategory = Water,
                ItemType	= base:drainable,
                /* container+contents (BACKLOG 3.19): was 0.2 -> 1.1.
                   Base.Sportsbottle 0.1 + 1.0 L */
                Weight = 1.1,
                Icon	=	Sportsbottle,

                CanStoreWater = TRUE,
                FillFromDispenserSound = GetWaterFromDispenser,
                FillFromTapSound = GetWaterFromTap,
                IsWaterSource = TRUE,
                ReplaceOnDeplete = Sportsbottle,
                ReplaceOnUseOn = WaterSource-SportsBottleFull,
                UseDelta = 0.1,
                UseWhileEquipped = FALSE,
                CustomEatSound = DrinkingFromBottlePlastic,
                StaticModel = Sportsbottle,
                WorldStaticModel = Sportsbottle,
        }


	item PopBottleWaterRefill

	{
	
    IsCookable		= TRUE,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
	
		DisplayCategory 	= Water,
		IsWaterSource		= TRUE,
		/* container+contents (BACKLOG 3.19): was 0.2 -> 2.1.
		   Base.PopBottle 0.1 + 2.0 L -- a 2-LITRE bottle */
		Weight			= 2.1,
		CanStoreWater		= TRUE,
		FillFromDispenserSound 	= GetWaterFromDispenserPlasticMedium,
		FillFromTapSound 	= GetWaterFromTapPlasticMedium,
		ItemType	= base:drainable,
		UseWhileEquipped	= FALSE,
		UseDelta		= 0.1,
		DisplayName		= Pop Bottle filled with Water,
		ReplaceOnDeplete	= PopBottle,
		ReplaceOnUseOn		= WaterSource-WaterPopBottle,
		Icon			= PopBottle,
		CustomContextMenu 	= Drink,
		CustomEatSound 		= DrinkingFromBottlePlastic,
		StaticModel 		= PopBottleWorld,
		WorldStaticModel 	= PopBottleWorld,
	}
	
	
	
	

	item BleachWaterRefill
	
	
	{
	
    IsCookable		= TRUE,
    RainFactor = 0.2,
    Tooltip = Tooltip_item_RainFromGround,
	
	    DisplayCategory = Water,
		IsWaterSource	=	TRUE,
		Icon	=	Bleach,
		/* container+contents (BACKLOG 3.19): was 0.7 -> 1.2.
		   Base.Bleach 0.2 + 1.0 L */
		Weight	=	1.2,
		CanStoreWater	=	TRUE,
		ItemType	= base:drainable,
		UseWhileEquipped	=	FALSE,
		UseDelta	=	0.1,
		DisplayName	=	Bleach Bottle filled with Water,
		ReplaceOnDeplete	=	Bleach,
		ReplaceOnUseOn	=	WaterSource-WaterBleachBottle,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottle,
		StaticModel = BleachBottle,
		WorldStaticModel = BleachBottle,
	}
	
	
	
	
	item KettleWaterRefill

	{

	    DisplayCategory = Water,
		IsWaterSource	=	TRUE,
		/* container+contents (BACKLOG 3.19): was 2 -> 2.5.
		   Base.Kettle 1.0 + 1.5 L */
		Weight	=	2.5,
		CanStoreWater	=	TRUE,
		FillFromDispenserSound = GetWaterFromDispenserMetalBig,
		FillFromTapSound = GetWaterFromTapMetalBig,
		ItemType	= base:drainable,
		UseWhileEquipped	=	FALSE,
		UseDelta	=	0.07,
		DisplayName	=	Kettle filled with Water,
		ReplaceOnDeplete	=	Kettle,
		ReplaceOnUseOn	=	WaterSource-FullKettle,
		Icon	=	Kettle,
		IsCookable	=	TRUE,
		StaticModel = Kettle,
		EatType = Kettle,
		WorldStaticModel = KettleGround,
		Tags = CoffeeMaker,
	}
	
	
	
item SaucepanWaterRefill

{
		DisplayName = Saucepan filled with Water,
		DisplayCategory = Water,
		ItemType	= base:drainable,
		/* container+contents (BACKLOG 3.19): was 2 -> 1.3.
		   Base.Saucepan 0.7 + 0.6 L -- DROPS, the pan only holds 600 ml */
		Weight = 1.3,
		Icon = SaucepanEmpty,
		CanStoreWater = TRUE,
		ConditionMax = 5,
		EatType = Kettle,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		IsCookable = TRUE,
		IsWaterSource = TRUE,
		RainFactor = 0.8,
		ReplaceOnDeplete = Saucepan,
		ReplaceOnUseOn = WaterSource-WaterSaucepan,
		Tooltip = Tooltip_item_RainFromGround,
		UseDelta = 0.05,
		UseWhileEquipped = FALSE,
		StaticModel = SaucePan,
		WorldStaticModel = SaucePan_World_Fluid,
		Tags = HasMetal,
}


	/* BACKLOG 2.8 (2026-09-06) -- the spirit half of bucket decanting, revived.
	   Originally shipped in acef84b and reverted in 3b7121c because the recipe
	   never consumed the bucket; quirks rule 65 explains that (a real
	   `component FluidContainer` input needs flags[ItemCount]) and the recipes
	   below now carry the flag.

	   NUTRITIONALLY THIS IS WhiskeyRefill, byte for byte (HungerChange -10,
	   Calories 1200, AlcoholPower 2, UseDelta 0.25 -> 4 sips).  That is
	   deliberate: no balance number is being invented here.  A bucket of
	   spirit is worth exactly one whiskey bottle and weighs 4.0 instead of
	   0.7, i.e. it is a strictly heavier container for the same drink, so
	   adding it cannot inflate the spirit economy.

	   Two fields differ from the 2026-08-26 original, both measured:
	     ReplaceOnUse      Base.BucketEmpty, not the bare `BucketEmpty`
	                       (rule 66: a bare token in this field is
	                       MODULE-scoped).  WhiteLightning already does this.
	     WorldStaticModel  Bucket_Ground_Fluid, not BucketFullWater_Ground --
	                       the latter resolves to nothing ("???." in
	                       ModelScript.check, see the sibling BucketWaterRefill
	                       which still has the dead reference). */
	item BucketRefill
	{
	    StressChange	=	-50,
	    DisplayCategory = Food,

		/* Weight 2.0 = container + contents (quirks rule 68), not an arbitrary
		   "heavier than a bottle" anti-exploit number. RefillSpiritInBucket3/4/5
		   (this file's recipes) each drain exactly ONE serving from the source
		   still pot (bare `item 1 [pot]`, rule 37) regardless of tier -- NOT the
		   whole 4/6/8-serving pot, so this is intentionally a single flat value,
		   not a 3-tier scale. "One serving" is independently pinned at 1 litre
		   by two sources: vanilla Base.EmptyJar's own component FluidContainer
		   declares Capacity = 1.0, and reference_b42_craftrecipe_quirks rule 67
		   / project_bucket_decant_done already measured "every vessel in the
		   family is about one serving big" = 1 L. Using the same water density
		   the report itself measured live (a sink-filled Base.BucketEmpty reads
		   11 = 1.0 container + 10 L water, i.e. 1.0 weight/L): 1.0 (bucket,
		   matching vanilla Base.BucketEmpty's own declared Weight) + 1.0 (one
		   1 L serving) = 2.0. Previous value (4.0) was carried over unchanged
		   from the older flat BucketWaterRefill/WateringcanWaterRefill items in
		   this same file and was never actually container+contents math. */
		Weight	=	2.0,

		ItemType	= base:food,
		FoodType = Liquor,

		UnhappyChange	=	-50,
		ThirstChange	=	-10,
		HungerChange	=	-10,

		DisplayName	= Bucket filled with Spirit,
		ReplaceOnUse	= Base.BucketEmpty,
		Alcoholic	=	TRUE,
		AlcoholPower = 2,
		Icon	=	MetalBucket_Water,
		CustomContextMenu = Drink,
		CustomEatSound = DrinkingFromBottleGlass,
		Carbohydrates = 1,
        Proteins = 0,
        Lipids = 0,
        Calories = 1200,
		Packaged = TRUE,
        CantBeFrozen = TRUE,
        StaticModel = Bucket,
        WorldStaticModel = Bucket_Ground_Fluid,
		EatType = Bucket,
		UseDelta = 0.25,
		Tags = HasMetal;Liquor,
	}


	item BucketWaterRefill

	{
	    DisplayCategory = Water,
		IsWaterSource	=	TRUE,
		/* Weight 11.0 = container + contents (2026-09-06 bucket sweep).
		   This is THE reference case the whole sweep is calibrated on: a
		   sink-filled vanilla Base.BucketEmpty reads exactly 11 in-game, and
		   the engine says why -- BucketEmpty declares Weight 1.0 and a
		   component FluidContainer with Capacity 10.0, and
		   InventoryItem.getContentsWeight() for a FluidContainer returns
		   fluidContainer.getAmount() verbatim, i.e. 1.0 kg per litre.
		   This item is a legacy base:drainable with no FluidContainer, so it
		   gets no automatic contents weight and the script Weight must be the
		   FULL figure.  DrainableComboItem.updateWeight() then interpolates
		   (11.0 - 1.0) * uses + 1.0 down to the empty bucket's own 1.0 as it
		   drains -- verified live (at Weight 4 it read 4.00 full / 2.56 half).
		   Was 4, which is neither the container, the contents, nor the sum:
		   it made a full 10 L bucket of water lighter than 4 litres of it. */
		Weight	=	11.0,
		CanStoreWater	=	TRUE,
		FillFromDispenserSound = GetWaterFromDispenserMetalBig,
		FillFromTapSound = GetWaterFromTapMetalBig,
		ItemType	= base:drainable,
		DisplayName	=	Bucket filled with Water,
		UseWhileEquipped	=	FALSE,
		UseDelta	=	0.04,
		ReplaceOnDeplete	=	BucketEmpty,
		ReplaceOnUseOn	=	WaterSource-BucketWaterFull,
		Icon	=	MetalBucket_Water,
		RainFactor = 1,
		Tooltip = Tooltip_item_RainFromGround,
		StaticModel = Bucket,
		EatType = Bucket,
		WorldStaticModel = Bucket_Ground_Fluid,
		IsCookable = TRUE,
		Tags = HasMetal,
	}
	
	
	  item WateringcanWaterRefill

	{
	DisplayCategory 	= Water,
	ItemType	= base:drainable,
	DisplayName		= Watering Can filled with Water,
	Icon			= WateringCan,
	/* Weight 10.0 = container + contents (2026-09-06 bucket sweep).
	   Measured live: Base.WateredCan declares Weight 2.0 and a component
	   FluidContainer with Capacity 8.0, so a full watering can is
	   2.0 + 8 L = 10.0.  Was 4.0, copied from the old flat BucketWaterRefill
	   value and never actually container+contents (the BucketRefill comment
	   further up this file already flagged 4.0 as that stray inheritance).
	   ReplaceOnDeplete = WateredCan means DrainableComboItem.updateWeight()
	   tapers this to 2.0 as it empties, so only the full figure is set here. */
	Weight			= 10.0,
    	UseDelta		= 0.025,
	UseWhileEquipped	= false,
    	ReplaceOnUseOn 		= WaterSource-WateredCanFull,
    	ReplaceOnDeplete 	= WateredCan,
    	IsWaterSource		= true,
	CanStoreWater		= true,
	FillFromDispenserSound 	= GetWaterFromDispenserPlasticBig,
	FillFromTapSound 	= GetWaterFromTapPlasticBig,
	IsCookable		= TRUE,
        RainFactor 		= 0.2,
        Tooltip 		= Tooltip_item_RainFromGround,
        StaticModel 		= WateringCan,
        WorldStaticModel 	= WateringCanGround,
        EatType 		= WateringCan,
		Tags = HasMetal,
	}
	
	
	
	
	 item JarWaterRefill
	 
    {
	DisplayCategory 	= Water,
	
	IsWaterSource		= TRUE,
	/* container+contents (BACKLOG 3.19): was 0.4 -> 1.2.
	   Base.EmptyJar 0.2 + 1.0 L */
	Weight			= 1.2,
	CanStoreWater		= TRUE,
	FillFromDispenserSound 	= GetWaterFromDispenserGlass,
	FillFromTapSound 	= GetWaterFromTapGlass,
	ItemType	= base:drainable,
	UseWhileEquipped	= FALSE,
	UseDelta		= 0.16,
	DisplayName		= Jar with Water (Full),
	ReplaceOnDeplete	= Base.EmptyJar,
	ReplaceOnUseOn		= WaterSource-JarWaterFull,
	Icon			=     ApaJar,
	/* Models added 2026-09-06 (BACKLOG 3.9).  This item had NEITHER
	   StaticModel nor WorldStaticModel -- the only vessel in the whole
	   pour-out family with no in-hand model at all (headless sweep:
	   56 of 57 resolved, this one returned nil).  Same pair that every
	   other jar-shaped item in this file already uses. */
	StaticModel 		= JarGlass_Ground,
	WorldStaticModel 	= JarGlass_Ground,

	CustomEatSound 		= DrinkingFromBottleGlass,
    }
	
	
	
	
	/*-----ENDs----*/
}