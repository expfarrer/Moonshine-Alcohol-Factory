module Moonshine
{
    imports
    {
        Base
    }

    craftRecipe MakePotatoMashInBucket
    {        /* Tainted-water gate: ALREADY ENFORCED by the `-fluid 0.1 [Water]` line below. No OnTest needed.
           Ground truth from CFR-decompiled 42.20 (2026-09-05, BACKLOG 6.2): `[Water]` makes InputScript
           build a FluidFilter WHITELIST containing the single FluidType.Water(1). TaintedWater(4) is a
           DISTINCT FluidType that merely shares the `Water` *category*, and FluidFilter.allows() needs
           typeMatch && categoryMatch -- with an empty category list categoryMatch defaults true but
           typeMatch stays false, so TaintedWater is refused. The default FluidMatchMode is Exact, which
           also requires !container.isMixture(), so a part-clean/part-tainted bottle is refused too.
           CraftRecipeManager.consumeInputFluidInternal() applies this on BOTH the testOnly=true
           (availability) and testOnly=false (consume) paths, so the recipe simply does not offer --
           it cannot silently succeed on dirty water.
           DO NOT "improve" this to `-fluid N categories[Water]`: the Water *category* DOES contain
           TaintedWater and that would reopen the hole.
           The 2026-08-27 note that used to sit here was correct that OnTest is a closed native registry
           (RecipeCodeOnTest is Java-only and has no notTaintedWater -- only the inverse, purifyWater),
           but it predates the rule-30 water gate (d9fe353, 2026-08-29) which restored this check for
           free. Evidence: tools/pzrepl/tests/taintedwater.lua. */

        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 200,
        NeedToBeLearn = true,
        SkillRequired = Cooking:1,
        xpAward = Cooking:10,
        inputs
        {
        item 1 tags[base:mixingutensil] mode:keep flags[Prop1],
        item 1 [Base.BucketEmpty] mode:destroy flags[ItemCount],
        item 1 [Base.WaterBottle] mode:keep,
        -fluid 0.1 [Water],
        item 1 [Base.Sugar;Base.SugarBrown] flags[ItemCount],
        item 1 [Base.Yeast],
        item 15 [Base.Potato] flags[ItemCount],
        }
        outputs
        {
            item 1 Moonshine.PotatoMash,
        }
    }

    craftRecipe MakeCornMashInBucket
    {        /* Tainted-water gate: ALREADY ENFORCED by the `-fluid 0.1 [Water]` line below. No OnTest needed.
           Ground truth from CFR-decompiled 42.20 (2026-09-05, BACKLOG 6.2): `[Water]` makes InputScript
           build a FluidFilter WHITELIST containing the single FluidType.Water(1). TaintedWater(4) is a
           DISTINCT FluidType that merely shares the `Water` *category*, and FluidFilter.allows() needs
           typeMatch && categoryMatch -- with an empty category list categoryMatch defaults true but
           typeMatch stays false, so TaintedWater is refused. The default FluidMatchMode is Exact, which
           also requires !container.isMixture(), so a part-clean/part-tainted bottle is refused too.
           CraftRecipeManager.consumeInputFluidInternal() applies this on BOTH the testOnly=true
           (availability) and testOnly=false (consume) paths, so the recipe simply does not offer --
           it cannot silently succeed on dirty water.
           DO NOT "improve" this to `-fluid N categories[Water]`: the Water *category* DOES contain
           TaintedWater and that would reopen the hole.
           The 2026-08-27 note that used to sit here was correct that OnTest is a closed native registry
           (RecipeCodeOnTest is Java-only and has no notTaintedWater -- only the inverse, purifyWater),
           but it predates the rule-30 water gate (d9fe353, 2026-08-29) which restored this check for
           free. Evidence: tools/pzrepl/tests/taintedwater.lua. */

        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 200,
        NeedToBeLearn = true,
        SkillRequired = Cooking:1,
        xpAward = Cooking:10,
        inputs
        {
        item 1 tags[base:mixingutensil] mode:keep flags[Prop1],
        item 1 [Base.BucketEmpty] mode:destroy flags[ItemCount],
        item 1 [Base.WaterBottle] mode:keep,
        -fluid 0.1 [Water],
        item 1 [Base.Sugar;Base.SugarBrown] flags[ItemCount],
        item 1 [Base.Yeast],
        item 15 [Base.Corn] flags[ItemCount],
        }
        outputs
        {
            item 1 Moonshine.CornMash,
        }
    }

    craftRecipe MakeCannedCornMashInBucket
    {        /* Tainted-water gate: ALREADY ENFORCED by the `-fluid 0.1 [Water]` line below. No OnTest needed.
           Ground truth from CFR-decompiled 42.20 (2026-09-05, BACKLOG 6.2): `[Water]` makes InputScript
           build a FluidFilter WHITELIST containing the single FluidType.Water(1). TaintedWater(4) is a
           DISTINCT FluidType that merely shares the `Water` *category*, and FluidFilter.allows() needs
           typeMatch && categoryMatch -- with an empty category list categoryMatch defaults true but
           typeMatch stays false, so TaintedWater is refused. The default FluidMatchMode is Exact, which
           also requires !container.isMixture(), so a part-clean/part-tainted bottle is refused too.
           CraftRecipeManager.consumeInputFluidInternal() applies this on BOTH the testOnly=true
           (availability) and testOnly=false (consume) paths, so the recipe simply does not offer --
           it cannot silently succeed on dirty water.
           DO NOT "improve" this to `-fluid N categories[Water]`: the Water *category* DOES contain
           TaintedWater and that would reopen the hole.
           The 2026-08-27 note that used to sit here was correct that OnTest is a closed native registry
           (RecipeCodeOnTest is Java-only and has no notTaintedWater -- only the inverse, purifyWater),
           but it predates the rule-30 water gate (d9fe353, 2026-08-29) which restored this check for
           free. Evidence: tools/pzrepl/tests/taintedwater.lua. */

        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 200,
        NeedToBeLearn = true,
        SkillRequired = Cooking:1,
        xpAward = Cooking:10,
        inputs
        {
        item 1 tags[base:mixingutensil] mode:keep flags[Prop1],
        item 1 [Base.TinOpener] mode:keep,
        item 1 [Base.BucketEmpty] mode:destroy flags[ItemCount],
        item 1 [Base.WaterBottle] mode:keep,
        -fluid 0.1 [Water],
        item 1 [Base.Sugar;Base.SugarBrown] flags[ItemCount],
        item 1 [Base.Yeast],
        item 2 [Base.CannedCorn] mode:destroy flags[ItemCount],
        }
        outputs
        {
            item 1 Moonshine.CornMash,
            item 2 Base.TinCanEmpty,
        }
    }

    craftRecipe MakeCannedCornMashInBucket2
    {        /* Tainted-water gate: ALREADY ENFORCED by the `-fluid 0.1 [Water]` line below. No OnTest needed.
           Ground truth from CFR-decompiled 42.20 (2026-09-05, BACKLOG 6.2): `[Water]` makes InputScript
           build a FluidFilter WHITELIST containing the single FluidType.Water(1). TaintedWater(4) is a
           DISTINCT FluidType that merely shares the `Water` *category*, and FluidFilter.allows() needs
           typeMatch && categoryMatch -- with an empty category list categoryMatch defaults true but
           typeMatch stays false, so TaintedWater is refused. The default FluidMatchMode is Exact, which
           also requires !container.isMixture(), so a part-clean/part-tainted bottle is refused too.
           CraftRecipeManager.consumeInputFluidInternal() applies this on BOTH the testOnly=true
           (availability) and testOnly=false (consume) paths, so the recipe simply does not offer --
           it cannot silently succeed on dirty water.
           DO NOT "improve" this to `-fluid N categories[Water]`: the Water *category* DOES contain
           TaintedWater and that would reopen the hole.
           The 2026-08-27 note that used to sit here was correct that OnTest is a closed native registry
           (RecipeCodeOnTest is Java-only and has no notTaintedWater -- only the inverse, purifyWater),
           but it predates the rule-30 water gate (d9fe353, 2026-08-29) which restored this check for
           free. Evidence: tools/pzrepl/tests/taintedwater.lua. */

        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 200,
        NeedToBeLearn = true,
        SkillRequired = Cooking:1,
        xpAward = Cooking:10,
        inputs
        {
        item 1 tags[base:mixingutensil] mode:keep flags[Prop1],
        item 1 [Base.BucketEmpty] mode:destroy flags[ItemCount],
        item 1 [Base.WaterBottle] mode:keep,
        -fluid 0.1 [Water],
        item 1 [Base.Sugar;Base.SugarBrown] flags[ItemCount],
        item 1 [Base.Yeast],
        item 2 [Base.CannedCornOpen] mode:destroy flags[ItemCount],
        }
        outputs
        {
            item 1 Moonshine.CornMash,
            item 2 Base.TinCanEmpty,
        }
    }

    craftRecipe MakeCannedPotatoMashInBucket
    {        /* Tainted-water gate: ALREADY ENFORCED by the `-fluid 0.1 [Water]` line below. No OnTest needed.
           Ground truth from CFR-decompiled 42.20 (2026-09-05, BACKLOG 6.2): `[Water]` makes InputScript
           build a FluidFilter WHITELIST containing the single FluidType.Water(1). TaintedWater(4) is a
           DISTINCT FluidType that merely shares the `Water` *category*, and FluidFilter.allows() needs
           typeMatch && categoryMatch -- with an empty category list categoryMatch defaults true but
           typeMatch stays false, so TaintedWater is refused. The default FluidMatchMode is Exact, which
           also requires !container.isMixture(), so a part-clean/part-tainted bottle is refused too.
           CraftRecipeManager.consumeInputFluidInternal() applies this on BOTH the testOnly=true
           (availability) and testOnly=false (consume) paths, so the recipe simply does not offer --
           it cannot silently succeed on dirty water.
           DO NOT "improve" this to `-fluid N categories[Water]`: the Water *category* DOES contain
           TaintedWater and that would reopen the hole.
           The 2026-08-27 note that used to sit here was correct that OnTest is a closed native registry
           (RecipeCodeOnTest is Java-only and has no notTaintedWater -- only the inverse, purifyWater),
           but it predates the rule-30 water gate (d9fe353, 2026-08-29) which restored this check for
           free. Evidence: tools/pzrepl/tests/taintedwater.lua. */

        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 200,
        NeedToBeLearn = true,
        SkillRequired = Cooking:1,
        xpAward = Cooking:10,
        inputs
        {
        item 1 tags[base:mixingutensil] mode:keep flags[Prop1],
        item 1 [Base.TinOpener] mode:keep,
        item 1 [Base.BucketEmpty] mode:destroy flags[ItemCount],
        item 1 [Base.WaterBottle] mode:keep,
        -fluid 0.1 [Water],
        item 1 [Base.Sugar;Base.SugarBrown] flags[ItemCount],
        item 1 [Base.Yeast],
        item 2 [Base.CannedPotato2] mode:destroy flags[ItemCount],
        }
        outputs
        {
            item 1 Moonshine.PotatoMash,
            item 2 Base.TinCanEmpty,
        }
    }

    craftRecipe MakeCannedPotatoMashInBucket2
    {        /* Tainted-water gate: ALREADY ENFORCED by the `-fluid 0.1 [Water]` line below. No OnTest needed.
           Ground truth from CFR-decompiled 42.20 (2026-09-05, BACKLOG 6.2): `[Water]` makes InputScript
           build a FluidFilter WHITELIST containing the single FluidType.Water(1). TaintedWater(4) is a
           DISTINCT FluidType that merely shares the `Water` *category*, and FluidFilter.allows() needs
           typeMatch && categoryMatch -- with an empty category list categoryMatch defaults true but
           typeMatch stays false, so TaintedWater is refused. The default FluidMatchMode is Exact, which
           also requires !container.isMixture(), so a part-clean/part-tainted bottle is refused too.
           CraftRecipeManager.consumeInputFluidInternal() applies this on BOTH the testOnly=true
           (availability) and testOnly=false (consume) paths, so the recipe simply does not offer --
           it cannot silently succeed on dirty water.
           DO NOT "improve" this to `-fluid N categories[Water]`: the Water *category* DOES contain
           TaintedWater and that would reopen the hole.
           The 2026-08-27 note that used to sit here was correct that OnTest is a closed native registry
           (RecipeCodeOnTest is Java-only and has no notTaintedWater -- only the inverse, purifyWater),
           but it predates the rule-30 water gate (d9fe353, 2026-08-29) which restored this check for
           free. Evidence: tools/pzrepl/tests/taintedwater.lua. */

        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 200,
        NeedToBeLearn = true,
        SkillRequired = Cooking:1,
        xpAward = Cooking:10,
        inputs
        {
        item 1 tags[base:mixingutensil] mode:keep flags[Prop1],
        item 1 [Base.BucketEmpty] mode:destroy flags[ItemCount],
        item 1 [Base.WaterBottle] mode:keep,
        -fluid 0.1 [Water],
        item 1 [Base.Sugar;Base.SugarBrown] flags[ItemCount],
        item 1 [Base.Yeast],
        item 2 [Base.CannedPotatoOpen] mode:destroy flags[ItemCount],
        }
        outputs
        {
            item 1 Moonshine.PotatoMash,
            item 2 Base.TinCanEmpty,
        }
    }

    craftRecipe MakeCannedPotatoMashInBucket3
    {        /* Tainted-water gate: ALREADY ENFORCED by the `-fluid 0.1 [Water]` line below. No OnTest needed.
           Ground truth from CFR-decompiled 42.20 (2026-09-05, BACKLOG 6.2): `[Water]` makes InputScript
           build a FluidFilter WHITELIST containing the single FluidType.Water(1). TaintedWater(4) is a
           DISTINCT FluidType that merely shares the `Water` *category*, and FluidFilter.allows() needs
           typeMatch && categoryMatch -- with an empty category list categoryMatch defaults true but
           typeMatch stays false, so TaintedWater is refused. The default FluidMatchMode is Exact, which
           also requires !container.isMixture(), so a part-clean/part-tainted bottle is refused too.
           CraftRecipeManager.consumeInputFluidInternal() applies this on BOTH the testOnly=true
           (availability) and testOnly=false (consume) paths, so the recipe simply does not offer --
           it cannot silently succeed on dirty water.
           DO NOT "improve" this to `-fluid N categories[Water]`: the Water *category* DOES contain
           TaintedWater and that would reopen the hole.
           The 2026-08-27 note that used to sit here was correct that OnTest is a closed native registry
           (RecipeCodeOnTest is Java-only and has no notTaintedWater -- only the inverse, purifyWater),
           but it predates the rule-30 water gate (d9fe353, 2026-08-29) which restored this check for
           free. Evidence: tools/pzrepl/tests/taintedwater.lua. */

        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 200,
        NeedToBeLearn = true,
        SkillRequired = Cooking:1,
        xpAward = Cooking:10,
        inputs
        {
        item 1 tags[base:mixingutensil] mode:keep flags[Prop1],
        item 1 [Base.BucketEmpty] mode:destroy flags[ItemCount],
        item 1 [Base.WaterBottle] mode:keep,
        -fluid 0.1 [Water],
        item 1 [Base.Sugar;Base.SugarBrown] flags[ItemCount],
        item 1 [Base.Yeast],
        item 2 [Base.CannedPotato;Base.CannedPotato_Open] mode:destroy flags[ItemCount],
        }
        outputs
        {
            item 1 Moonshine.PotatoMash,
            item 2 Base.EmptyJar,
        }
    }

    craftRecipe CoverCornMashBucketWithTarp
    {
        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 300,
        inputs
        {
        item 1 [Moonshine.CornMash] flags[ItemCount],
        item 1 [Base.Tarp] flags[Prop1],
        item 1 [Base.RubberBand;Base.RubberHose;Base.Rope;Base.Wire],
        }
        outputs
        {
            item 1 Moonshine.CornMashCovered,
        }
    }

    craftRecipe CoverPotatoMashBucketWithTarp
    {
        timedAction = Making, category = Moonshine,
        Tags = InHandCraft;Moonshine;CanBeDoneFromFloor,
        time = 300,
        inputs
        {
        item 1 [Moonshine.PotatoMash] flags[ItemCount],
        item 1 [Base.Tarp] flags[Prop1],
        item 1 [Base.RubberBand;Base.RubberHose;Base.Rope;Base.Wire],
        }
        outputs
        {
            item 1 Moonshine.PotatoMashCovered,
        }
    }
}
